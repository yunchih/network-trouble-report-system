var request = require('request');

var base = "https://localhost:8080/api/1.0/";

function User(user){
    var obj = user;
    var token = "";
    this.prototype = {
        login: function(){
            
            request.post( base + "auth/login",
                          { qs:
                            { access_token: }
                          });
            
        },
        
        register: function(){},
        getCurrent: function(){},
        updateCurrent: function(){}
    };

    
    
};